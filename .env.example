# Database Configuration
# For local development: sqlite:///./authy.db
# For Docker/production: postgresql://user:password@db/authy
DATABASE_URL=postgresql://user:password@localhost/authy

# Encryption key for 2FA secrets (32-byte base64 encoded)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-32-byte-base64-encoded-key

# Docker environment variables
POSTGRES_DB=authy
POSTGRES_USER=user
POSTGRES_PASSWORD=your-secure-password-here

# OIDC/SSO Configuration (optional)
OIDC_CLIENT_ID=your-client-id
OIDC_CLIENT_SECRET=your-client-secret
OIDC_ISSUER_URL=https://your-oidc-provider

# SMTP Email Configuration (optional)
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-password

# CORS Configuration for production
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:8040,http://127.0.0.1:8040